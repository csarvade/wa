pipeline {
    agent any
    environment{
     PATH="C:\apache-maven-3.9.6:$PATH"
    }
    stages {
        stage('clone') {
            steps {
             echo 'Cloning..'
             git credentialsId: 'git credentials', url:  'https://github.com/csarvade/wa.git'
                
            }
        }
         stage('Build') {
            steps {
                echo 'Building..'
                sh "mvn clean install"
             }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
                 cd C:\tom\apache-tomcat-9.0.86\bin
                 sh shutdown.sh
                 scp **/*.war C:\tom\apache-tomcat-9.0.86\webapps\
                 sh startup.sh
            }
        }
    }
}
