pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                cmd git checkout https://github.com/csarvade/wa.git
                cmd mvn clean install
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
                cmd cp **/*.war webapps/
            }
        }
    }
}
