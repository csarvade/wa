pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                cmd mvn clean install
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
                cmd cp **/*.war webapps/
            }
        }
    }
}
